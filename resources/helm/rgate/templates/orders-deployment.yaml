apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.orders.name }}
  labels:
    app: {{ .Values.orders.name }}
spec:
  replicas: {{ .Values.orders.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.orders.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.orders.name }}
    spec:
      containers:
      - name: {{ .Values.orders.name }}
        image: {{ .Values.orders.ImageName }}
        ports:
        - containerPort: {{ .Values.orders.containerPort }}
