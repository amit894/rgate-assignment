apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.payments.name }}
  labels:
    app: {{ .Values.payments.name }}
spec:
  ports:
    - port: {{ .Values.payments.servicePort }}
      targetPort: {{ .Values.payments.containerPort }}
      protocol: TCP
  selector:
    app: {{ .Values.payments.name }}
