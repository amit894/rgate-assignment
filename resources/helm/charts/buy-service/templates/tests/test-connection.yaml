apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "buy-service.fullname" . }}-test-connection"
  labels:
    {{- include "buy-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "buy-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
